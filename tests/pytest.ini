[pytest]
markers =
    redis: run redis ut
    mysql: run mysql ut
    mysql_user_repo: run mysql user repo ut
    mysql_url_repo: run mysql url repo ut

filterwarnings =
    ignore:.*the imp module is deprecated.*:DeprecationWarning