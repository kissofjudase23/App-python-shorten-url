Delete the existing user
---
tags:
  - Url

consumes:
  - application/json

parameters:
  - name: user_id
    in: path
    type: string
  - name: url_id
    in: path
    type: string

responses:
  '200':
    description: "delete the url"
  '500':
    description: "internal server error"
